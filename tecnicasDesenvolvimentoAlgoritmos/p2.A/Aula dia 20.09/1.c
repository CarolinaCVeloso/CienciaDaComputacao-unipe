/* 

*/


#include    <stdio.h>
#include    <locale.h>


int main()
{
    setlocale (0, "Portuguese");

    int x1, y1, x2, y2, x3, y3, x4, y4;

    printf ("Digite a coordenada X, Y: ");
    scanf("%d", &qtd_coordenadas);

    printf ("Digite a coordenada")

}


    if(qtd_coordenadas == 0){
        printf ("Erro, não foram informadas coordenadas ");    
    } else if (qtd_coordenadas == 1) {
       // pronto
    } else if (qtd_coordenadas == 2) {
        // Reta
    } else{
        if ((x1 == x4) && (y1 == y4)){
            //poligono
        } else {
            //linha

        }  
    }
}
